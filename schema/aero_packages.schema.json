{
  "$schema": "https://spec.openapis.org/oas/3.1/meta/base",
  "$id": "aero_packages",
  "title": "Aero Packages",
  "description": "Aerodynamic package of a vehicle",
  "type": "object",
  "properties": {
    "print_name": {
      "description": "Printable name of the aerodynamic package",
      "type": "string"
    },
    "frontal_area": {
      "description": "The frontal area of the aerodynamic package",
      "type": "number",
      "exclusiveMinimum": 0
    },
    "front_aero_distribution": {
      "description": "The proportion of downforce acting on the front axle",
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "aero_model": {
      "description": "Model of the aerodynamic package",
      "type": "object",
      "properties": {
        "model_type": {
          "description": "Type of aerodynamic model described",
          "enum": ["constant"]
        },
        "lift_coefficient": {
          "description": "The lift coefficient of the aerodynamic package",
          "type": "number",
          "exclusiveMinimum": 0
        },
        "drag_coefficient": {
          "description": "The drag coefficient of the aerodynamic package",
          "type": "number",
          "exclusiveMinimum": 0
        }
      },
      "required": ["model_type", "lift_coefficient", "drag_coefficient"],
      "additionalProperties": false
    },
    "lift_coefficient": {
      "description": "The lift coefficient of the aerodynamic package",
      "type": "number",
      "exclusiveMinimum": 0
    },
    "drag_coefficient": {
      "description": "The drag coefficient of the aerodynamic package",
      "type": "number",
      "exclusiveMinimum": 0
    },
    "mass": {
      "description": "The mass of the aerodynamic package",
      "type": "number",
      "exclusiveMinimum": 0
    }
  },
  "required": [
    "print_name",
    "frontal_area",
    "front_aero_distribution",
    "aero_model"
  ],
  "additionalProperties": false
}
